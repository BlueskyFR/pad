{
  mongoUrl: "mongodb://localhost:27017",
  dbName: "pad",
  collectionName: "pads",

  saveInterval: 10, // In minutes

  // The delay after which a pad can be unloaded from the memory
  unloadDelay: 10,

  // A password that allows access to all the pads
  adminPassword: "jaimelesbites74"
}
